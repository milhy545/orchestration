# Migration Archives

This folder stores migration-specific backup artifacts and transition snapshots.

- Use for before/after migration backups and one-time transition captures.
- Prefer names in the form `component-migration-backup-YYYYMMDD-HHMMSS.ext`.
- Keep only artifacts that help reconstruct or audit a migration path.
