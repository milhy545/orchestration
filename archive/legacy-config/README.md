# Legacy Config

This folder stores preserved config variants and backup config files that are no longer active.

- Use it for old config snapshots kept for reference or rollback context.
- Do not point active tooling at files in this folder.
